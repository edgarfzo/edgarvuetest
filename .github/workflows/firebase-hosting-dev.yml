# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on DEV
on:
  push:
    branches:    
      - 'dev/*'

jobs:
  build_and_deploy_dev:
    runs-on: ubuntu-latest
    environment: 'development'
    steps:
      - uses: actions/checkout@v2
        env:
          VUE_APP_ENV: ${{vars.VUE_APP_ENV}}
          VUE_APP_API_KEY: ${{vars.UE_APP_API_KEY}}
          VUE_APP_AUTH_DOMAIN: ${{vars.VUE_APP_AUTH_DOMAIN}}
          VUE_APP_DB_URL: ${{vars.VUE_APP_DB_URL}}
          VUE_APP_PROJECT_ID: ${{vars.VUE_APP_PROJECT_ID}}
          VUE_APP_STORAGE_BUCKET: ${{ vars.VUE_APP_STORAGE_BUCKET}}
          VUE_APP_MSG_SENDER_ID: ${{vars.VUE_APP_MSG_SENDER_ID}}
          VUE_APP_ID: ${{vars.VUE_APP_ID}}
          VUE_APP_MEASURE_ID: ${{vars.VUE_APP_MEASURE_ID}}
      - run: echo ${{vars.VUE_APP_PROJECT_ID}}
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TEST_E4100 }}'
          channelId: live
          projectId: test-e4100


